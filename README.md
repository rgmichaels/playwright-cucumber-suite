cat > README.md << 'EOF'
# Playwright + TypeScript + Cucumber Suite

A lightweight, scalable UI test framework using:
- Playwright
- TypeScript
- Cucumber (Gherkin)
- Page Objects

## Setup
```bash
npm install
npx playwright install
